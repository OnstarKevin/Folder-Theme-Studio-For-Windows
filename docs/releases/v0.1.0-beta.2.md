# Folder Theme Studio v0.1.0-beta.2

Beta 2 修复了两项恢复安全与结果报告问题。程序仍然只用于设计和替换普通文件夹图标，不修改文件资源管理器界面、文件类型图标或 Windows 特殊文件夹图标。

## 本版修复

- 如果兼容模式创建的 `Desktop.ini` 无法安全删除或持久记录，应用会保留准确的未解决路径、将回滚标为未完成，并禁止写入持久完成标记。
- 如果文件夹恢复已经执行，但后续 Shell 刷新或恢复完成标记失败，结果仍会保留成功、跳过和发生变化的逐路径明细。

## 主要功能

- 高度可定制的玻璃霓虹主题编辑器。
- 浅色/深色背景与四种尺寸实时预览。
- 当前用户全局快速模式。
- 仅处理显式本地目录的兼容模式。
- 应用前规划、恢复快照、回滚和失败路径导出。
- 自包含 `win-x64` 程序，无需另装 .NET 运行时。

## 自动验证

- 完整 Release 自动测试通过。
- 自包含发布、ZIP 内容、EXE 文件头与 SHA-256 会在发布前重新检查。

## 重要提示

- 这是预发布版本，二进制未签名，Windows 可能显示 SmartScreen 提示。
- Windows 10/11、不同 DPI、Explorer、真实应用/恢复、UI Automation 与屏幕阅读器尚未人工验收。

建议先在一次性测试目录中使用兼容模式，并保留应用生成的恢复数据。
