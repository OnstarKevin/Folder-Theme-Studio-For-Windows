# Folder Theme Studio v0.1.0-beta.1

首个公开 Beta 版本，用于设计并替换普通文件夹图标，不修改文件资源管理器界面、文件类型图标或 Windows 特殊文件夹图标。

## 包含内容

- 高度可定制的玻璃霓虹主题编辑器。
- 浅色/深色背景与四种尺寸实时预览。
- 当前用户全局快速模式。
- 仅处理显式本地目录的兼容模式。
- 应用前规划、恢复快照、回滚和失败路径导出。
- 自包含 `win-x64` 程序，无需另装 .NET 运行时。

## 自动验证

- Release 自动测试：172 项。
- 自包含发布与 ZIP/SHA-256 产物检查。

## 重要提示

- 这是预发布版本，二进制未签名，Windows 可能显示 SmartScreen 提示。
- 极窄的二次记录失败路径中，未跟踪或已变化的 `Desktop.ini` 可能在残留时被误报为已经回滚。
- 文件夹恢复完成后，如果 Shell 刷新或持久完成标记失败，返回结果可能丢失已经完成的逐文件夹明细。
- Windows 10/11、不同 DPI、Explorer、真实应用/恢复、UI Automation 与屏幕阅读器尚未人工验收。

建议先在一次性测试目录中使用兼容模式，并保留应用生成的恢复数据。
